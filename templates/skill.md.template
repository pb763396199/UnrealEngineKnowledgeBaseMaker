---
name: ue5kb-{ENGINE_VERSION}
description: UE{ENGINE_VERSION} 引擎源码知识库 - 查询模块、类、函数信息。当任务涉及虚幻引擎 {ENGINE_VERSION} 源码的编译错误、代码修改、审查、定位、调试、API/类型/函数确认时，必须先执行 KB gate（preflight → branch update-all if stale → impl.py 查询），不要先用 Glob/Grep/read_file 直接搜源码；仅当 KB 明确 miss 后才可降级到源码搜索。
---

# UE{ENGINE_VERSION} 引擎源码知识库查询

## 何时使用此技能

以下任务场景都必须先执行 KB gate，**再**使用 Glob/Grep/read_file 搜索源码：

| 场景 | 说明 |
|------|------|
| 编译错误/类型错误 | 确认类型声明、函数签名、头文件所在模块 |
| 代码修改/新增 | 确认要修改的函数签名、继承关系、模块依赖 |
| 代码审查 | 确认 API 用法是否正确、依赖是否缺失 |
| 调试/定位 | 确认调用链、函数实现位置、符号引用 |
| API/类/函数确认 | 任何需要查找引擎源码内符号的操作 |

**KB gate 顺序（强制）**：`preflight` → stale 则 `branch update-all` → `impl.py` 查询 → KB miss 时才降级到 Glob/Grep/read_file。

**重要**: 始终使用下面的 CLI 命令查询知识库，不要先用 Glob/Grep/read_file 搜源码。KB 未找到目标时，明确标注"KB miss"后再降级。


### 模块相关查询
- "Core 模块有哪些依赖？"
- "列出所有 Runtime 模块"
- "搜索包含 'Network' 的模块"
- "Engine 模块依赖哪些其他模块？"

### 类相关查询
- "AActor 类的继承关系是什么？"
- "UObject 类有哪些方法？"
- "查找包含 'Character' 的类"
- "FString 类在哪个模块？"
- "哪些类继承自 AActor？"

### 函数相关查询
- "BeginPlay 函数的定义是什么？"
- "GetWorld 函数在哪些模块中定义？"
- "查找 Blueprint 可调用的函数"
- "ProcessEvent 函数的签名是什么？"

## 如何查询（重要）

**必须使用 CLI 命令查询知识库，不要使用 Glob/Grep 搜索源码。**

### 查询步骤

1. **源码问答前先执行 preflight（必须）:**
   ```bash
   py "{SKILL_PATH}/impl.py" preflight
   ```
   如需批量确认所有分支是否 current，先执行：
   ```bash
   py -m ue5_kb.cli branch update-all --check --skill-path "{SKILL_PATH}"
   ```
   如果输出中 `data_trust=stale` 或 `fresh=false`，或检查结果不是 `current`，先执行：
   ```bash
   py -m ue5_kb.cli branch update-all --skill-path "{SKILL_PATH}"
   ```
   更新完成后再次执行 `preflight`，确认 `fresh=true` 且关键索引存在。

2. **使用 Bash 工具执行 impl.py 脚本，可传 trace-id 串联审计:**
   ```bash
   py "{SKILL_PATH}/impl.py" [--trace-id <id>] <command> [args...]
   ```

3. **解析返回的 JSON 结果**

4. **向用户展示结果**

### 可用 CLI 命令

| 命令 | 参数 | 说明 |
|------|------|------|
| `preflight` | - | 检查 KB/source/indices/runtime context，新鲜度不足先刷新 |
| `query_class_info` | `<class_name>` | 查询类的详细信息 |
| `query_class_hierarchy` | `<class_name> [depth=3]` | 查询类的继承层次 |
| `query_module_dependencies` | `<module_name>` | 查询模块依赖关系 |
| `query_module_classes` | `<module_name> [class_type]` | 查询模块中的所有类 |
| `query_function_info` | `<function_name> [module_hint]` | 查询函数信息（包含声明和实现位置） |
| `get_function_implementation` | `<function_name> [class_name] [module_hint]` | 获取函数的完整实现代码 |
| `source_slice` | `<relative_file> <line> [context\|function\|context_lines] [context_lines] [max_context_lines] [max_output_chars]` | 安全读取 `source_root` 内源码切片；第三参为数字时按 context 半径兼容处理 |
| `search_files` | `<keyword> [limit=20]` | 搜索源码文件全文索引，返回行号锚点；FTS5 不可用时降级 LIKE |
| `search_functions` | `<keyword> [limit=50]` | 搜索包含关键字的函数 |
| `search_classes` | `<keyword> [limit=50]` | 搜索包含关键字的类 |
| `search_modules` | `<keyword>` | 搜索包含关键字的模块 |
| `query_subclasses` | `<parent_class> [limit=100]` | 查询继承自指定类的所有子类 |
| `query_module_dependents` | `<module_name>` | 查询依赖指定模块的所有模块（反向依赖） |
| `query_enum_info` | `<enum_name> [module_hint]` | 查询枚举信息（含枚举值） |
| `search_enums` | `<keyword> [limit=50]` | 搜索枚举 |
| `query_examples` | `<target_name> [limit=5]` | 查询代码使用示例 |
| `query_audit` | `[trace_id] [--limit N]` 或 `[N]` | 查看 `memory/memory.sqlite` 中的查询审计记录；单个数字参数按 limit 处理 |
| `query_audit_report` | `[trace_id] [limit=200] [broad_threshold=40]` | 基于审计记录生成确定性静态统计报告，包含失败率、宽泛搜索和验收状态 |
| `query_memory_record` | `<trace_id> <intent> <seed>` | 把一轮查询 trace 或有价值片段记录为可复用路线，不保存业务事实 |
| `query_memory_search` | `<keyword> [intent] [limit=20]` | 搜索已沉淀路线，也返回自动审计日志中的 trace 片段候选 |
| `query_memory_validate` | `<memory_id>` | 纯算法校验历史路线是否对当前 KB/源码仍新鲜 |
| `query_memory_replay` | `<memory_id>` | 在当前 KB 上重跑历史路线 |
| `query_memory_diff` | `<memory_id>` | 输出历史路线和当前查询结果的差异 |
| `query_memory_promote` | `<trace_id> <intent> <seed>` | 把当前 trace 提升为推荐查询路线 |
| `query_memory_subjects` | `[keyword] [limit=50]` | 查看 evidence-bound 业务主题视图；subject 不是事实主键 |
| `query_memory_negative_hints` | `[keyword] [limit=50]` | 查看隔离失败避坑提示；不参与主图事实检索 |
| `query_memory_snapshot` | `<subject_id\|pattern_id>` | 保存 subject 或 pattern 的完整 JSON 历史快照 |
| `query_memory_render` | `[subject\|pattern\|all] [view=full]` | 从 `memory/memory.sqlite` 程序化生成 Obsidian Markdown/Mermaid；`full` 保持主文档 |
| `query_memory_render_graph` | `[subject\|pattern\|all] [business\|evidence\|evolution\|query-path]` | 生成独立 graph 视图：业务、证据、演进、查询路径 |
| `query_memory_attach_flow` | `<memory_id> <flow.json>` | 把本轮已验证证据归纳出的业务流程图附加到 memory；返回质量评分和 `publication_status`，Markdown 只默认展示 `published` 泳道业务图 |
| `query_callees` | `<function_name> [class_name] [limit=50] [resolution_state=resolved]` | 查询函数调用目标，默认仅 resolved |
| `query_callers` | `<function_name> [class_name] [limit=50] [resolution_state=resolved]` | 查询调用某函数的位置，默认仅 resolved |
| `query_symbol_references` | `<symbol_name> [limit=50] [resolution_state=resolved]` | 查询符号引用，默认仅 resolved |
| `resolve_seed` | `<seed> [limit=20]` | 查询前确定性解析 seed 是 function/class/struct/substring/miss |
| `symbol_reference_report` | `<symbol_name> [limit=50] [resolution_state=all]` | 静态符号引用报表 |
| `symbol_evidence_bundle` | `<symbol_name> [limit=20] [resolution_state=all]` | 固定 schema 静态证据包 |
| `code_flow_index` | `[limit=100] [resolution_state=all]` | 静态引用图边视图，不复制 KB |
| `query_static_closure` | `<seed> [depth=3] [limit=80] [resolution_state=all]` | 有界静态探索导航，返回 `closure_validation`、缺口和可执行 `next_static_command_objects` |
| `query_flow` | `<seed> [direction] [edge_types] [depth] [limit] [resolution_state]` | 静态图遍历 |
| `trace_business_flow` | `<seed> [depth=4] [limit=160] [resolution_state=resolved] [evidence_limit=12]` | 实验性静态证据聚合器，返回内部调用统计、证据切片、frontier 和 Mermaid 流程图；不能作为完整业务闭环证明 |
| `trace_flow_path` | `<from> <to> [depth] [limit] [resolution_state]` | 静态路径查询 |
| `get_statistics` | - | 获取知识库统计信息 |

### 命令分组速查

| 分组 | 优先命令 | 常用程度 | 什么时候用 |
|---|---|---|---|
| 查询前检查 | `preflight` | 高 | 每次源码问答、修改、审查、调试前先确认 KB 可信 |
| 基础结构 | `resolve_seed`, `query_class_info`, `query_function_info` | 高 | 先确认名字、类型、声明和实现位置 |
| 源码证据 | `get_function_implementation`, `source_slice` | 高 | 最终回答必须回到这些源码证据 |
| 模糊搜索 | `search_classes`, `search_functions`, `search_modules`, `search_files` | 中 | 精确名字未知或 KB 明确 miss 后使用 |
| 静态关系 | `query_callees`, `query_callers`, `query_symbol_references`, `symbol_reference_report`, `symbol_evidence_bundle` | 高 | 查调用、引用、上下游和证据包 |
| 业务探索 | `query_static_closure`, `query_flow`, `trace_flow_path` | 高/中 | 复杂链路用它们导航，再回源验证 |
| 实验聚合 | `trace_business_flow` | 实验 | 首轮证据聚合和画图；不能作为完整业务闭环证明 |
| 审计复盘 | `query_audit`, `query_audit_report` | 中/高 | 统计本轮调用次数、失败率和宽泛搜索 |
| 查询记忆 | `query_memory_search`, `query_memory_validate`, `query_memory_replay`, `query_memory_record` | 高 | 复用已沉淀路线和自动审计 trace 片段，但必须重跑当前事实 |
| 记忆可视化 | `query_memory_subjects`, `query_memory_attach_flow`, `query_memory_render`, `query_memory_render_graph`, `query_memory_snapshot`, `query_memory_negative_hints` | 中/高 | 查看业务证据图谱、保存业务流程附注、生成 Obsidian 图、导出多视图 graph、归档快照、复查避坑 |
| 版本统计 | `get_statistics`, `get_kb_info` | 中 | 确认 KB 规模、版本和来源 |

### 常见任务路径

| 任务 | 推荐路径 |
|---|---|
| 查一个类 | `preflight` → `resolve_seed` → `query_class_info` → `symbol_evidence_bundle` |
| 查一个函数 | `preflight` → `query_function_info` → `get_function_implementation` → `query_callees/query_callers` |
| 查复杂业务链 | `preflight` → `resolve_seed` → `query_static_closure` → 按 `next_static_command_objects` 继续 `query_flow/symbol_evidence_bundle/source_slice` |
| 复用历史业务路线 | `preflight` → `query_memory_search` → 看 `results` 和 `audit_traces` → `query_memory_validate`/`query_memory_replay` → 继续查 `expanded/changed` 缺口 |
| 沉淀业务流程图 | 完成源码证据核查 → `query_memory_record` → 写入 flow.json（可声明 `quality_baseline`）→ `query_memory_attach_flow` → 确认 `publication_status=published` → `query_memory_render` → 打开 Markdown 复查泳道图、节点证据和关键实体覆盖 |
| 不确定名字 | `search_classes/search_functions/search_modules` → 找到准确符号后回到结构化查询 |
| 效率复盘 | 查询时加 `--trace-id` → `query_audit_report` |

### 查询示例

```bash
# 预检 KB/source/indices
py "{SKILL_PATH}/impl.py" preflight

# 查询类信息
py "{SKILL_PATH}/impl.py" --trace-id demo query_class_info AActor

# 查询类继承关系（深度5层）
py "{SKILL_PATH}/impl.py" query_class_hierarchy AActor 5

# 查询模块依赖
py "{SKILL_PATH}/impl.py" query_module_dependencies Core

# 搜索类（限制20个结果）
py "{SKILL_PATH}/impl.py" search_classes Player 20

# 安全读取源码函数块
py "{SKILL_PATH}/impl.py" source_slice Source/Runtime/Engine/Private/Actor.cpp 120 function

# 兼容读取指定行上下文半径：第三个数字表示 context_lines
py "{SKILL_PATH}/impl.py" source_slice Source/Runtime/Engine/Private/Actor.cpp 120 16

# 搜索源码文件
py "{SKILL_PATH}/impl.py" search_files RHICreateTexture 20

# 查看最近 20 条审计记录；查询同一 trace 时把 trace_id 放在 --limit 前
py "{SKILL_PATH}/impl.py" query_audit --limit 20

# 生成确定性静态审计统计报告，用于评估查询效率和失败率
py "{SKILL_PATH}/impl.py" query_audit_report my-trace-id 200 40

# 将本轮 trace 提升为历史查询路线；复用前必须 validate/replay
py "{SKILL_PATH}/impl.py" query_memory_record my-trace-id explain_business_flow FeatureLayer
py "{SKILL_PATH}/impl.py" query_memory_search FeatureLayer explain_business_flow 10
py "{SKILL_PATH}/impl.py" query_memory_validate <memory_id>
py "{SKILL_PATH}/impl.py" query_memory_replay <memory_id>
py "{SKILL_PATH}/impl.py" query_memory_subjects FeatureLayer
py "{SKILL_PATH}/impl.py" query_memory_negative_hints FeatureLayer
py "{SKILL_PATH}/impl.py" query_memory_snapshot <subject_id_or_pattern_id>
py "{SKILL_PATH}/impl.py" query_memory_attach_flow <memory_id> flow.json
py "{SKILL_PATH}/impl.py" query_memory_render FeatureLayer
py "{SKILL_PATH}/impl.py" query_memory_render_graph FeatureLayer business
py "{SKILL_PATH}/impl.py" query_memory_render_graph FeatureLayer evidence
py "{SKILL_PATH}/impl.py" query_memory_render_graph FeatureLayer evolution
py "{SKILL_PATH}/impl.py" query_memory_render_graph FeatureLayer query-path

# 静态证据包与引用图
py "{SKILL_PATH}/impl.py" resolve_seed FMyFeatureFragment
py "{SKILL_PATH}/impl.py" symbol_reference_report BeginPlay 20 all
py "{SKILL_PATH}/impl.py" symbol_evidence_bundle BeginPlay 10 resolved
py "{SKILL_PATH}/impl.py" query_static_closure FeatureLayer 3 80 all
py "{SKILL_PATH}/impl.py" query_flow BeginPlay out call 1 20 all
py "{SKILL_PATH}/impl.py" trace_business_flow FeatureLayer 4 160 resolved 12

# 获取统计信息
py "{SKILL_PATH}/impl.py" get_statistics
```

## 使用建议

1. **KB gate 优先（编译/修改/审查/调试均适用）**：不论是回答问题还是执行编译修复、代码修改、审查、调试，都必须先走 KB gate，不要先 Glob/Grep/read_file 扫源码
2. **先做 preflight**：每次任务开始前先运行 `preflight`；发现 `stale` 必须先更新再查询
3. **始终使用 CLI 查询**：不要直接使用 Glob/Grep 搜索源码，知识库查询更快更准确
4. **优先使用精确查询**：如果知道具体的类名或模块名，使用 `query_class_info` 或 `query_module_dependencies`
5. **使用搜索功能**：如果只知道关键字，使用 `search_modules`、`search_classes` 或 `search_functions`
6. **KB miss 时才降级**：KB 未找到目标时，明确标注"KB miss"，再降级到 Glob/Grep/read_file 源码搜索
7. **处理错误结果**：如果返回的 JSON 包含 `error` 字段，说明查询失败
8. **不要把 partial/truncated 当作完成**：`query_static_closure` 的 `closure_validation.status == "PASS"` 只表示当前索引、预算和规则范围内探索完成；不能证明完整业务真理
9. **保持 static-only**：查询命令不得调用 LLM、RAG、embedding 或 vector search；`source_slice`、`search_files` 只能作为证据回源，不能伪装成语义边
10. **复杂业务不要声称一条命令闭环**：`trace_business_flow` 只能做首轮静态证据聚合；它的 `automation_score.status` 和 `closure_validation.status` 不会返回 `PASS`，不得作为“完整业务逻辑/一查到底”的验收门
11. **不要混淆两种闭包**：`trace_business_flow.coverage.closed` 固定为 `false`，表示它不是业务闭环证明；`formal_semantic_closure == false` 时，不能宣传成全局形式化语义闭包证明
12. **复杂业务回答必须引用源码证据**：即使 `query_static_closure` 给出下一步建议，也要用 `source_slice` 或 `get_function_implementation` 验证关键入口、分支、注册、回调和终点
13. **历史记忆先复用路线，再沉淀业务流程**：`query_memory_search` 会暴露自动审计 trace 片段，`query_memory_record/promote` 把有价值片段沉淀为稳定路线；完成业务解释后必须把本轮归纳出的泳道业务图写成 flow.json 并调用 `query_memory_attach_flow`，否则 Markdown 只能退回静态调用图，不能代表完整业务流程
14. **业务流程附注必须过质量门槛**：`query_memory_attach_flow` 会给出确定性评分和 `publication_status`；低于 flow.json 声明的 `quality_baseline` 或通用默认基线时只保存为 `candidate`，默认 Markdown 不展示；必须补齐节点、边、泳道、分支条件、节点证据和关键实体覆盖后重挂成 `published`
15. **业务流程附注不是原始事实源**：`query_memory_attach_flow` 保存的是基于当前源码证据归纳出的业务图；最终事实仍必须能由 `query_memory_validate/replay`、文件 hash、anchor hash 和当前 `source_slice/get_function_implementation` 复查
16. **Obsidian Markdown 是生成物**：`query_memory_render` 只从 `memory/memory.sqlite` 生成可视化 Markdown/Mermaid，不能把 MD 当事实源手写维护；需要改格式时重新生成

---

## 查询失败处理（重要）

当精确查询返回 `{"error": "未找到..."}` 时，**必须**按以下降级策略执行：

### 查询降级流程

1. **精确查询失败** → 使用模糊搜索
2. **需要源码细节** → 优先用 `source_slice` 或 `search_files` 回源验证
3. **模糊搜索失败** → 明确告知用户"知识库中未找到该信息"

### 具体降级命令

| 精确查询 | 失败后使用 |
|---------|-----------|
| `query_class_info` | `search_classes <关键字>` |
| `query_function_info` | `search_functions <关键字>` |
| `query_module_dependencies` | `search_modules <关键字>` |

### 示例对话

**错误做法（禁止）**：
```
用户: "RHICreateTexture2D 怎么用？"
助手: [query_function_info 返回 error]
助手: "RHICreateTexture2D 是一个旧版 API..." ❌ 基于训练数据乱回答
```

**正确做法**：
```
用户: "RHICreateTexture2D 怎么用？"
助手: [query_function_info RHICreateTexture2D → error]
助手: [search_functions RHICreate] → 找到 RHICreateTexture 等
助手: "知识库中未找到 RHICreateTexture2D，但找到相关函数：RHICreateTexture、RHICreateTexture2DArray..."
```

### 关键原则

- **严禁在查询失败时基于训练数据回答** - 这会导致过时或错误的信息
- **必须使用工具搜索** - 模糊搜索找不到时，明确告知用户
- **源码问答必须先 preflight** - stale 时先刷新，不能绕过 KB 直接猜
- **大结果用 query_audit 追踪** - 默认审计只记录命令、参数摘要和结果数量，不记录源码全文
- **实战验收用 query_audit_report** - 对复杂业务探索必须统计失败事件、broad search 和 acceptance 状态；该报告只基于静态审计记录，不引入 LLM/RAG
- **返回错误信息中的提示** - 如果 error 包含 `fallback_command` 字段，必须执行

---

## 知识库路径

知识库位于: 优先由 `{SKILL_PATH}/registry.db` 动态解析到 `variants/<commit>`；不要依赖旧的固定 `KnowledgeBase` 路径。

Skill 路径: `{SKILL_PATH}`

### 跨 Agent 部署约定

- 原始 KB 只应有一份，位于共享 Skill store 的 `{SKILL_PATH}/variants/<commit>`。
- Claude Code / OpenCode 入口可以是目录链接或 junction，目标仍是 `{SKILL_PATH}`。
- Codex / VS Code Copilot 入口应是轻量 adapter，调用 `{SKILL_PATH}/impl.py`，不要复制 `variants/`。
- 不要手写或依赖引擎源码目录内的 `KnowledgeBase`；需要确认路径时运行 `preflight`。
- Windows 控制台可能不是 UTF-8，命令输出必须按 JSON 解析，不要要求生成器输出非 ASCII 状态符号。

## 示例对话

**用户**: "AActor 类继承自什么？"
**助手**:
```bash
py "{SKILL_PATH}/impl.py" query_class_hierarchy AActor
```
→ 解析 JSON 结果并展示继承链

**用户**: "Core 模块有哪些依赖？"
**助手**:
```bash
py "{SKILL_PATH}/impl.py" query_module_dependencies Core
```
→ 解析 JSON 结果并展示依赖列表

**用户**: "查找所有包含 PlayerController 的类"
**助手**:
```bash
py "{SKILL_PATH}/impl.py" search_classes PlayerController
```
→ 解析 JSON 结果并展示匹配的类

---

## CPP 文件索引功能

此知识库支持 CPP 文件索引，可以获取函数的实现位置。

### 获取函数实现

使用 `get_function_implementation` 命令直接获取函数的 C++ 实现代码：

```bash
# 基本用法
py "{SKILL_PATH}/impl.py" get_function_implementation "Tick"

# 指定类名（处理同名函数）
py "{SKILL_PATH}/impl.py" get_function_implementation "Tick" "AActor"

# 指定模块提示（加速查询）
py "{SKILL_PATH}/impl.py" get_function_implementation "Tick" "AActor" "Engine"
```

返回结果包含：
- `declaration_file`: 声明位置（.h 头文件）
- `implementation_file`: 实现位置（.cpp 文件）
- `file_content`: 完整的 cpp 文件内容（用于理解函数实现）

### 查询函数信息

`query_function_info` 现在返回函数的实现位置：

```bash
py "{SKILL_PATH}/impl.py" query_function_info "BeginPlay"
```

返回结果包含：
- `file`: 声明文件路径（.h）
- `line`: 声明行号
- `impl_file`: 实现文件路径（.cpp）
- `impl_line`: 实现行号

---

## 知识库版本信息

此技能生成的知识库信息：
- **引擎版本**: {ENGINE_VERSION}
- **KB 格式版本**: {KB_VERSION}
- **工具版本**: {TOOL_VERSION}
- **创建时间**: {CREATED_AT}
- **最后更新**: {LAST_UPDATED}

如需检查知识库是否为最新版本，比较引擎版本即可。

查询知识库版本信息：
```bash
py "{SKILL_PATH}/impl.py" get_kb_info
```
